# Egg.js 源码揭秘（一）：架构总览

> 本文从宏观角度介绍 Egg.js 的整体架构，帮你建立全局视角。

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         Master 进程                          │
│                                                             │
│  • 负责 fork Agent 和 Worker                                │
│  • 进程间通信（IPC）                                         │
│  • 进程守护和重启                                            │
└─────────────────────────────────────────────────────────────┘
        │                                    │
        │ fork                               │ fork (N个)
        ▼                                    ▼
┌───────────────────┐              ┌───────────────────┐
│    Agent 进程      │              │   Worker 进程      │
│                   │              │                   │
│  • 后台任务        │◄────IPC────►│  • 处理 HTTP 请求  │
│  • 定时任务        │              │  • 业务逻辑        │
│  • 长连接维护      │              │  • 多个实例        │
└───────────────────┘              └───────────────────┘
```

## 二、核心模块关系

```
┌─────────────────────────────────────────────────────────────┐
│                        egg (主框架)                          │
│                                                             │
│  Application ─────────────────────────────────────────────  │
│       │                                                     │
│       │ extends                                             │
│       ▼                                                     │
│  EggApplicationCore ──────────────────────────────────────  │
│       │                                                     │
│       │ extends                                             │
│       ▼                                                     │
└───────┼─────────────────────────────────────────────────────┘
        │
┌───────┼─────────────────────────────────────────────────────┐
│       ▼                      core (核心)                     │
│                                                             │
│  EggCore ◄──────── extends ──────── KoaApplication         │
│     │                                                       │
│     ├── loader: EggLoader      # 加载器                     │
│     ├── lifecycle: Lifecycle   # 生命周期                   │
│     ├── router: Router         # 路由                       │
│     └── config: EggAppConfig   # 配置                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 三、启动流程

```
pnpm run dev
      │
      ▼
egg-bin dev
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│                    Master.#start()                          │
├─────────────────────────────────────────────────────────────┤
│  1. parseOptions()          # 解析启动参数                   │
│  2. detectPorts()           # 检测可用端口                   │
│  3. forkAgentWorker()       # 启动 Agent                    │
│  4. forkAppWorkers()        # 启动 Worker（Agent 就绪后）    │
│  5. ready()                 # 所有进程就绪                   │
└─────────────────────────────────────────────────────────────┘
      │
      ├──────────────────────────────────────┐
      ▼                                      ▼
┌─────────────────────┐            ┌─────────────────────┐
│   Agent Worker      │            │    App Worker       │
├─────────────────────┤            ├─────────────────────┤
│  new Agent()        │            │  new Application()  │
│      │              │            │      │              │
│      ▼              │            │      ▼              │
│  loader.load()      │            │  loader.load()      │
│      │              │            │      │              │
│      ▼              │            │      ▼              │
│  lifecycle.ready()  │            │  server.listen()    │
└─────────────────────┘            └─────────────────────┘
```

## 四、Loader 加载顺序

EggLoader 按照固定顺序加载各类文件：

```
loadPlugin()          # 1. 加载插件配置
      │
      ▼
loadConfig()          # 2. 加载应用配置
      │
      ▼
loadApplicationExtend()  # 3. 加载 app 扩展
loadRequestExtend()      # 4. 加载 request 扩展
loadResponseExtend()     # 5. 加载 response 扩展
loadContextExtend()      # 6. 加载 context 扩展
loadHelperExtend()       # 7. 加载 helper 扩展
      │
      ▼
loadCustomLoader()    # 8. 自定义加载器
      │
      ▼
loadService()         # 9. 加载 Service
      │
      ▼
loadMiddleware()      # 10. 加载中间件
      │
      ▼
loadController()      # 11. 加载 Controller
      │
      ▼
loadRouter()          # 12. 加载路由
```

## 五、生命周期

```
┌─────────────────────────────────────────────────────────────┐
│                      Lifecycle                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  configWillLoad    ──► 配置即将加载，最后修改配置的机会      │
│        │                                                    │
│        ▼                                                    │
│  configDidLoad     ──► 配置加载完成                         │
│        │                                                    │
│        ▼                                                    │
│  didLoad           ──► 所有文件加载完成（异步）              │
│        │                                                    │
│        ▼                                                    │
│  willReady         ──► 即将就绪（异步）                      │
│        │                                                    │
│        ▼                                                    │
│  didReady          ──► 应用就绪                             │
│        │                                                    │
│        ▼                                                    │
│  serverDidReady    ──► 服务器启动完成                        │
│        │                                                    │
│        ▼                                                    │
│  beforeClose       ──► 应用关闭前                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 生命周期钩子示例

```typescript
// app.ts
import { Boot } from 'egg';

export default class AppBoot extends Boot {
  configWillLoad() {
    // 修改配置
    this.app.config.custom = 'value';
  }

  async didLoad() {
    // 初始化数据库连接
    await this.app.mysql.init();
  }

  async willReady() {
    // 预热缓存
    await this.app.cache.warmup();
  }

  async didReady() {
    // 应用已就绪
    this.app.logger.info('App is ready!');
  }

  async beforeClose() {
    // 清理资源
    await this.app.mysql.close();
  }
}
```

## 六、核心类

### 6.1 EggCore

继承自 Koa，是所有 Egg 应用的基类：

```typescript
class EggCore extends KoaApplication {
  options: EggCoreOptions;
  timing: Timing;
  lifecycle: Lifecycle;
  loader: EggLoader;
  
  // 路由代理
  get router(): Router;
  get(path, ...middleware): this;
  post(path, ...middleware): this;
  
  // 生命周期
  ready(): Promise<void>;
  close(): Promise<void>;
  
  // 单例
  addSingleton(name, create): void;
}
```

### 6.2 Application

Worker 进程中的单例，处理 HTTP 请求：

```typescript
class Application extends EggApplicationCore {
  server?: http.Server;
  Helper: typeof Helper;
  
  // 视图 locals
  get locals(): Record<string, any>;
  
  // 后台任务
  runInBackground(scope): void;
  
  // Cookie 密钥
  get keys(): string[];
}
```

### 6.3 Agent

Agent 进程中的单例，处理后台任务：

```typescript
class Agent extends EggApplicationCore {
  // Agent 特有的消息方法
  messenger: {
    broadcast(action, data): void;
    sendToApp(action, data): void;
    sendRandom(action, data): void;
  };
}
```

## 七、进程间通信

```
┌─────────────────────────────────────────────────────────────┐
│                        Messenger                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Agent ◄─────────────────────────────────────► Worker      │
│    │                                              │         │
│    │  sendToApp(action, data)                     │         │
│    │  ─────────────────────────────────────────►  │         │
│    │                                              │         │
│    │  sendToAgent(action, data)                   │         │
│    │  ◄─────────────────────────────────────────  │         │
│    │                                              │         │
│    │  broadcast(action, data)                     │         │
│    │  ◄────────────────────────────────────────►  │         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 通信示例

```typescript
// Agent 发送消息给所有 Worker
this.messenger.sendToApp('config-updated', { key: 'value' });

// Worker 发送消息给 Agent
this.messenger.sendToAgent('task-complete', { taskId: 123 });

// 广播给所有进程
this.messenger.broadcast('refresh-cache', {});
```

## 八、配置合并

配置按照以下顺序合并（后面覆盖前面）：

```
plugin config.default
    │
    ▼
framework config.default
    │
    ▼
app config.default
    │
    ▼
plugin config.{env}
    │
    ▼
framework config.{env}
    │
    ▼
app config.{env}
```

## 九、目录约定

```
app/
├── controller/          # 控制器
│   └── home.ts         # HomeController
├── service/            # 服务
│   └── user.ts         # UserService
├── middleware/         # 中间件
│   └── auth.ts         # 认证中间件
├── extend/             # 扩展
│   ├── application.ts  # app 扩展
│   ├── context.ts      # ctx 扩展
│   ├── request.ts      # request 扩展
│   ├── response.ts     # response 扩展
│   └── helper.ts       # helper 扩展
├── router.ts           # 路由配置
└── schedule/           # 定时任务
    └── sync.ts
```

## 十、小结

Egg.js 的核心设计：

1. **多进程架构**：Master + Agent + Worker，充分利用多核
2. **Loader 机制**：约定式加载，统一目录结构
3. **生命周期**：清晰的启动和关闭流程
4. **插件系统**：高度可扩展，按需加载
5. **配置合并**：多环境配置，灵活覆盖

---

> 📦 源码地址：[github.com/eggjs/egg](https://github.com/eggjs/egg)
> 
> 下一篇：多进程模型详解
> 
> 如果觉得有帮助，欢迎点赞收藏 👍
